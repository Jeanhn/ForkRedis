# rds



to add: 
    client-end: int input
    timer
    db select
    del key
    expire key
    aof
    rdb

to test:
    timer thread
    concurrent reqs

BUG:
    zset. almost all (add some cout to check)
